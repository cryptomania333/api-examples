### Prerequisites
Working Elixir installation

### Running the example
Inside the terminal: 

```bash
mix deps.get
```

start the elixir's interactive shell:

```bash
iex -S mix
```

inside iex:

```elixir
CryptowarpGetdata.get_data()
```
